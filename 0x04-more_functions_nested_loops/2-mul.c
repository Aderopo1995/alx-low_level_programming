#include "main.h"
/**
 * mul - a function that multiplies two integers.
 * @a: An input integer
 * @b: An input integer
 * Result: Always 0
 *
 * Return: the multiplication of the two integers
*/
int mul(int a, int b)
{
return (a * b);
}
